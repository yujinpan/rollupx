import { Vue } from 'vue-property-decorator';
/**
 * 组件
 * @class
 */
export default class HelloWorld extends Vue {
  private msg;
  /**
   * 构造函数
   * @param {string} str
   */
  constructor(str: any);
  /**
   * 测试
   */
  test(): void;
}
